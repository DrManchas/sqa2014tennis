language: python
python:
  - "3.4"
# command to install dependencies
install: "pip install -r requirements.txt"
# command to run tests
script: 
  - lettuce tests
  - nosetests --with-doctest --with-coverage -s tests
